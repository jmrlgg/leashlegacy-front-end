FROM node:20-alpine

WORKDIR /app

# Install deps first (better caching)
COPY package*.json ./
RUN npm ci

# Copy the rest
COPY . .

EXPOSE 4321

# Astro dev server
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "4321"]
